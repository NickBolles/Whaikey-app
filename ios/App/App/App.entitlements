<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
		Universal Links: https://<host>/* opens the app instead of Safari, so a
		shared bottle link lands on the bottle page. Requires
		/.well-known/apple-app-site-association to be served from that host; until
		it is, links fall back to the browser, so this is safe to ship early.

		Host and APS environment come from build settings (project.pbxproj) so the
		production domain is a build input and Debug builds get the sandbox push
		environment automatically.
	-->
	<key>com.apple.developer.associated-domains</key>
	<array>
		<string>applinks:$(APP_LINK_HOST)</string>
	</array>
	<key>aps-environment</key>
	<string>$(APS_ENVIRONMENT)</string>
</dict>
</plist>
